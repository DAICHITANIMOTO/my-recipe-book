# スマホから写真を送る設定（初回だけ・15分）

この設定が済むと、外食のあと **写真アプリの共有ボタン → ショートカット** の2タップで
料理写真がレシピ本に送られ、その晩のうちに下書きレシピ（推定の材料・作り方入り）が
自動でできあがる。

やることは2つ。**ステップ1（GitHubトークン発行）**と**ステップ2（ショートカット作成）**。

---

## ステップ1：GitHubトークンを発行する

写真をリポジトリに送るための「鍵」を1つ作る。スマホのブラウザでもPCでもよい。

1. https://github.com/settings/personal-access-tokens/new を開く（要ログイン）
   - これは **Fine-grained token**（新しい方式・権限を絞れる）のページ
2. 次のように設定する：
   | 項目 | 値 |
   |---|---|
   | Token name | `recipe-book-upload` |
   | Expiration | `1 year`（またはカスタムで2027-09-07あたり） |
   | Resource owner | `DAICHITANIMOTO` |
   | Repository access | 「Only select repositories」→ **`my-recipe-book`** を選ぶ |
3. 「Permissions」→「Repository permissions」を開き、**Contents** を **`Read and write`** にする
   （他の権限はすべて `No access` のままでよい。Metadata は自動で Read-only になる）
4. 一番下の「Generate token」を押す
5. 表示された文字列（`github_pat_` で始まる長い文字列）を**コピーする**
   → **この画面を離れると二度と表示されない**。メモアプリに一時的に貼っておく

> 有効期限は1年。切れると写真アップロードが失敗するようになる。
> そのときは同じ手順で作り直して、ショートカットのトークンを差し替える。
> （期限が近づいたら分かるように、別途リマインドを仕込む予定）

---

## ステップ2：ショートカットを作る

iPhone標準の「ショートカット」アプリで新規ショートカットを作り、
下のアクションを上から順に追加する。

### ショートカットの設定（歯車アイコン）
- 名前：`レシピ本に送る`
- 「共有シートに表示」を **オン**
- 「共有シートで受け取る項目」を **「イメージ」** のみにする

### アクション（上から順に）

1. **「日付」** アクション → そのまま（＝現在の日時）
2. **「日付をフォーマット」**
   - 入力：手順1の日付
   - フォーマット：カスタム → `yyyy-MM-dd_HHmmss`
   - この結果を長押しして変数名を **`Stamp`** にする
3. **「繰り返す（各項目）」**
   - 対象：`ショートカットの入力`（共有された写真たち）
   - ↓ここから手順4〜6は「繰り返す」の中に入れる
4. **「Base64エンコード」**
   - 入力：`繰り返し項目`（＝写真1枚）
5. **「URLの内容を取得」**
   - URL：
     ```
     https://api.github.com/repos/DAICHITANIMOTO/my-recipe-book/contents/recipes/inbox/[Stamp]_[繰り返しインデックス].jpg
     ```
     （`[Stamp]` と `[繰り返しインデックス]` は変数として挿入する。文字で打たない）
   - 「詳細を表示」を開いて：
     - 方法：**PUT**
     - ヘッダ：
       | キー | 値 |
       |---|---|
       | `Authorization` | `Bearer github_pat_ここにステップ1のトークン` |
       | `Accept` | `application/vnd.github+json` |
     - 本文（リクエストボディ）：**JSON** を選び、次の3キーを追加
       | キー | 型 | 値 |
       |---|---|---|
       | `message` | テキスト | `写真アップロード [Stamp]` |
       | `content` | テキスト | 手順4の `Base64エンコード` の結果（変数） |
       | `branch` | テキスト | `main` |
6. （繰り返しの終わり）
7. **「通知を表示」**
   - テキスト：`レシピ本に送りました`

これで完成。

---

## 使い方（毎回）

1. 外食で料理を撮る
2. 写真アプリでその写真を開く →「共有」ボタン → **「レシピ本に送る」**
3. 「レシピ本に送りました」と出れば成功
4. その晩、自動エージェントが下書きレシピを作る
5. 翌朝以降、PCで `git pull` すると新しいレシピが増えている

### 料理名・店名も一緒に送りたい場合（任意）

ショートカットの「繰り返す」の中、手順4の前に次を足す：

- **「テキストを要求」** 「料理名は？」→ 変数 `料理名`（空欄OK）
- **「テキストを要求」** 「店名は？」→ 変数 `店名`（空欄OK）

そして手順5と同じ「URLの内容を取得」をもう1つ追加して、
URLの拡張子を `.txt` に、JSONの `content` を
「`料理名: [料理名]` 改行 `店名: [店名]`」をBase64エンコードしたものにする。
（面倒なら、店名はPCであとから書き足すのでも十分）

---

## うまくいかないとき

- 通知は出るが翌朝レシピが増えない → GitHubの `my-recipe-book` → `recipes/inbox/` に
  画像が入っているか確認。入っていれば夜間エージェント側の問題。入っていなければショートカット側。
- 「URLの内容を取得」で失敗する → トークンの権限（Contents: Read and write）と
  Authorizationヘッダの `Bearer ` の後ろにトークンが正しく入っているか確認。
- それでもダメなら、ヘッダに `User-Agent` = `recipe-shortcut` を足す。
