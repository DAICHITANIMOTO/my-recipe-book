# マイレシピ本 — 作業ルール

外食で美味しかった料理を「再現レシピ」として残すための個人リポジトリ。

## 写真の取り込み方

- **自動**：ユーザーがGitHubの「Upload files」ページから料理写真を `recipes/inbox/` にコミットする。
  毎晩1回、クラウドの定期エージェントが `build/自動レシピ生成手順.md` に従って下書きレシピ＋
  サイトを作り push する。手動セッションで「未処理の写真をレシピにして」と言われたときも、この手順書に従う。
  （Issue添付画像はクラウド環境のプロキシで取得不可なので使わない）
- **手動（この会話で写真を直接渡された）**：下の「写真が送られてきたときの処理」に従う。

## 写真が送られてきたときの処理

ユーザーが料理の写真（＋料理名・店名・食べた日など）を送ってきたら、以下を実行する。
質問で確認を挟まず、分かる範囲で埋めて進める。

1. **日付を決める**：ユーザーが食べた日を言っていればそれを使う。無ければ今日の日付。
   形式は `YYYY-MM-DD`。

2. **ファイル名を決める**：`recipes/YYYY-MM-DD_料理名.md`
   - 料理名は日本語。スペースや `/` は使わない（例: `2026-09-06_ガスパチョ.md`）
   - 同名ファイルが既にあれば末尾に `_2` を付ける

3. **写真を保存する**：送られた画像を `recipes/画像/` に、Markdownと同じ名前で保存する
   （例: `recipes/画像/2026-09-06_ガスパチョ.jpg`）。拡張子は元画像に合わせる。

4. **レシピ本文を作る**：`レシピ雛形.md` をベースに以下を埋める。
   - frontmatter:
     - `title`: 料理名
     - `date`: 食べた日
     - `restaurant`: 店名（不明なら空欄のまま）
     - `category`: 写真と料理名から推定（前菜／タパス／メイン／魚／肉／パスタ／スープ／サラダ／デザート など）
   - 本文1行目に固定で:
     `> ※この材料・作り方は写真と料理名からの推定です。実際に作って調整してください。`
   - 画像リンク: `![料理名](画像/保存したファイル名)`
   - `## 説明`: 写真とユーザーのメモから2〜4文
   - `## 材料（2人分）`: 写真と料理名から**推定**して箇条書き（分量つき）
   - `## 作り方`: 写真と料理名から**推定**して番号付き手順
   - `## メモ・感想`: 空のまま（`（食べたときの感想をここに書く）`）
   - `## 調整メモ`: 空のまま（`（実際に作ってみて気づいたことをここに追記する）`）

5. **サイトを再生成してコミット＆push する**：
   ```
   python3 build/サイト生成.py
   git add -A
   git commit -m "レシピ追加: 料理名"
   git push
   ```
   push まで完了したら「push した」と一言で報告する。

## やらないこと

- 既存の他レシピファイルは編集しない
- `出力/` の中身はビルドの生成物なので触らない
- 1レシピ = Markdown 1つ ＋ 画像1つ（同じファイル名）を崩さない

## ビルド（PDF / Word 生成）

`python3 build/build.py` で `recipes/*.md` を全部まとめて `出力/マイレシピ本.pdf` と
`出力/マイレシピ本.docx` を生成する。必要ツールは `brew install pandoc typst`。
